{
  "sessionStart": {
    "commands": [
      {
        "command": "bash",
        "args": ["workplace/setup.sh"],
        "description": "Run workplace setup script to configure environment and install dependencies"
      }
    ]
  }
}
